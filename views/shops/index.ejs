<h1>All Shops</h1>

<div class="row">
    <% shops.forEach(s => { %>
        <div class="col s3">
            <div class="card">
                <a href="/shops/<%=s.id%>">
                    <%= s.name %>
                </a>
            </div>
        </div>
    <% }) %>
</div>